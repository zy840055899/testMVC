<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="com.test.dao.StaffDao">

    <select id="getAllStaffInfos" resultType="com.test.entity.Staff">
        SELECT * FROM staff WHERE status='1'
    </select>

    <select id="getSumStaffs" resultType="java.lang.Integer">
      SELECT count(id) FROM staff WHERE status='1'
    </select>

    <update id="deleteStaffById" parameterType="string">
      UPDATE staff SET status='0' WHERE id=#{_id}
    </update>

    <insert id="addStaff" parameterType="com.test.entity.Staff">
      INSERT INTO staff(name, age, phone, salary, addr,status)
      VALUES (#{name}, #{age}, #{phone}, #{salary}, #{addr}, '1')
    </insert>

    <update id="updateStaff" parameterType="com.test.entity.Staff">
        UPDATE staff
        SET name=#{name}, age=#{age}, phone=#{phone}, salary=#{salary}, addr=#{addr}
        WHERE id=#{id}
    </update>

</mapper>


